#!/bin/bash

function add() {
	FILE=$1
	if [ -f "$FILE" ]; then
		echo "\nalias blat='sudo'\n" >> $FILE
		source $FILE
		exit 0
	fi
}

add ~/.zshrc
add ~/.bash_profile
add ~/.bash_login 
add ~/.profile


